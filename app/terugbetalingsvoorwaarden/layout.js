import { pageMetadata } from "@/lib/pageMetadata";

export const metadata = pageMetadata("repaymentTerms");

export default function TerugbetalingsvoorwaardenLayout({ children }) {
  return children;
}
