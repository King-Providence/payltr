import { pageMetadata } from "@/lib/pageMetadata";

export const metadata = pageMetadata("useCaseMarketing");

export default function MarketingFinancieringLayout({ children }) {
  return children;
}
